

export enum UnaryOperator {
    // Math
    LogicNot,           // !
    Negate,             // -
    Not,                // not

    // Predicate
    Is,                 // is
    In,                 // in
    Or,                 // or
    // Not, (Not in)
}
